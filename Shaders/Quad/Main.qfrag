void main()
{
    GlobalData global_data = GlobalData_buffer.elems[0];
    QuadData quad_data = QuadData_buffer.elems[v_quad_data_index];

    switch(quad_data.m_Mode)
    {
        default:
            o_color = quad_data.m_Color;
            return;

        // case statements filled in dynamically