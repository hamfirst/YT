void main()
{
    GlobalData global_data = GlobalDataBuffer.elems[0];
    QuadData quad_data = GetQuadData(v_quad_data);

    switch(quad_data.m_Mode)
    {
        default:
            o_color = vec4(v_color.x, v_color.y, s, 1.0);
            return;

        // case statements filled in dynamically